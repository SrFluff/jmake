COMPILER = @CC@
CFLAGS = @CFLAGS@
TARGET = bin/jmake
SOURCE = src/jmake.c
INSTALLDIR = /usr/local/bin

all:
	$(COMPILER) $(CFLAGS) -o $(TARGET) $(SOURCE) -lcjson

install:
	install -m 755 $(TARGET) $(INSTALLDIR)

clean:
	rm $(TARGET)
	rm src/buildinfo.h
	rm Makefile
